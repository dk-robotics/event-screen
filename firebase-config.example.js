// You'll need to copy / paste this file to the same directory,
// and rename the new file to 'firebase-config.js'. Afterwards add the required Firebase config data

const firebaseConfig = {
    apiKey: "<API_KEY>",
    authDomain: "<PROJECT_ID>.firebaseapp.com",
    databaseURL: "https://<DATABASE_NAME>.firebaseio.com",
    storageBucket: "<BUCKET>.appspot.com",
}

export default firebaseConfig
