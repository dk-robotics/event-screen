{
  "name": "event-screen",
  "version": "1.0.0",
  "description": "Information screen for robot events",
  "main": "index.js",
  "repository": "git@github.com:dk-robotics/event-screen.git",
  "author": "viktorstrate <viktorstrate@gmail.com>",
  "license": "MIT",
  "scripts": {
    "build": "rm -rf dist/ && NODE_ENV=production parcel build src/index.html --public-url https://dk-robotics.github.io/event-screen/",
    "start": "parcel src/index.html",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "classnames": "^2.2.5",
    "gh-pages": "^1.0.0",
    "parcel": "^1.10.1"
  },
  "devDependencies": {
    "babel-core": "^6.26.3",
    "babel-preset-env": "^1.7.0"
  }
}
